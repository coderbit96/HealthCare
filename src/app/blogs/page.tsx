import Link from "next/link";
import { PageShell } from "@/components/public/page-shell";
const posts = [{ slug: "heart-health-everyday", title: "Small habits for a healthier heart" }, { slug: "when-to-see-a-doctor", title: "When a symptom deserves attention" }, { slug: "family-health-checks", title: "Planning your family health checks" }];
export default function Blogs() { return <PageShell><main className="mx-auto max-w-6xl px-5 py-20"><p className="font-semibold uppercase tracking-widest text-brand">Health journal</p><h1 className="mt-3 text-5xl font-semibold">Clearer health information.</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{posts.map(post => <Link className="rounded-2xl bg-brand-soft p-7" key={post.slug} href={`/blogs/${post.slug}`}><p className="text-sm font-semibold text-brand">Health insights</p><h2 className="mt-3 text-xl font-semibold">{post.title}</h2><p className="mt-5 text-sm text-ink-muted">Read article →</p></Link>)}</div></main></PageShell>; }
