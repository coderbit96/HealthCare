import type { Metadata } from "next";
import { PageShell } from "@/components/public/page-shell";
import { publicMetadata } from "@/lib/seo";
const humanize = (slug: string) => slug.split("-").map((part) => part[0]?.toUpperCase() + part.slice(1)).join(" ");
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const title = humanize(slug); return publicMetadata({ title, description: `${title}: evidence-informed health guidance from the Health Care Pvt. Ltd. clinical team.`, path: `/blogs/${slug}`, type: "article" }); }
export default async function BlogDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = humanize(slug); return <PageShell><article className="mx-auto max-w-3xl px-5 py-20"><p className="font-semibold uppercase tracking-widest text-brand">Health insights</p><h1 className="mt-3 text-5xl font-semibold">{title}</h1><p className="mt-8 text-lg leading-8 text-ink-muted">Our clinical teams share practical, evidence-informed information to help you make confident choices about your health. This article is for general education and does not replace personalised medical advice.</p></article></PageShell>; }
