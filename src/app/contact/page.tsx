import { MapPin, Phone } from "lucide-react";
import { PageShell } from "@/components/public/page-shell";
export default function Contact() { return <PageShell><main className="bg-canvas"><section className="mx-auto grid max-w-6xl gap-8 px-5 py-20 lg:grid-cols-2"><div><p className="font-semibold uppercase tracking-widest text-brand">Contact</p><h1 className="mt-3 text-5xl font-semibold">We’re here to help.</h1><p className="mt-6 text-lg leading-8 text-ink-muted">For appointments, enquiries and directions, contact our care team.</p><div className="mt-8 grid gap-4">{[[Phone, "+91 33 4000 2000"], [MapPin, "12 Wellness Avenue, Kolkata 700001"]].map(([Icon, text]) => { const ContactIcon = Icon as typeof Phone; return <p className="flex gap-3 font-semibold" key={text as string}><ContactIcon className="text-brand" />{text as string}</p>; })}</div></div><div className="min-h-80 rounded-3xl bg-surface-sunken p-8"><p className="font-semibold text-ink">Map area</p><p className="mt-2 text-sm text-ink-muted">Replace with your preferred maps integration for production.</p></div></section></main></PageShell>; }
