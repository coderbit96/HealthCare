import { Clock3, Phone, ShieldCheck } from "lucide-react";
import { AppointmentForm } from "@/components/public/appointment-form";
import { PageShell } from "@/components/public/page-shell";
export const metadata = { title: "Book an appointment | Health Care .Pvt .Ltd" };
export default function Appointments() { return <PageShell><main className="bg-canvas"><section className="mx-auto grid max-w-7xl gap-12 px-5 py-16 lg:grid-cols-[.8fr_1.2fr] lg:px-8 lg:py-20"><div><p className="text-sm font-semibold uppercase tracking-[.2em] text-brand">Appointments</p><h1 className="mt-3 font-display text-5xl font-semibold tracking-tight text-ink">Let’s get you the care you need.</h1><p className="mt-5 max-w-md text-lg leading-8 text-ink-muted">Send us your preferred time and department. Our care team will confirm your appointment directly.</p><div className="mt-10 grid gap-5">{[[Clock3, "Quick confirmation", "We’ll respond during working hours."], [Phone, "Need to speak now?", "+91 33 4000 2000"], [ShieldCheck, "Your privacy matters", "Your request is handled securely."]].map(([Icon, title, text]) => { const ItemIcon = Icon as typeof Clock3; return <div className="flex gap-4" key={title as string}><div className="h-fit rounded-xl bg-brand-soft p-3 text-brand"><ItemIcon size={20} /></div><div><h2 className="font-semibold text-ink">{title as string}</h2><p className="text-sm text-ink-muted">{text as string}</p></div></div>; })}</div></div><AppointmentForm /></section></main></PageShell>; }
