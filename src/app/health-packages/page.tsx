import { PageShell } from "@/components/public/page-shell";
import { HEALTH_PACKAGES } from "@/lib/site-content";
export default function Packages() { return <PageShell><main className="bg-canvas"><section className="mx-auto max-w-6xl px-5 py-20"><p className="font-semibold uppercase tracking-widest text-brand">Preventive care</p><h1 className="mt-3 text-5xl font-semibold">Health packages with purpose.</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{HEALTH_PACKAGES.map(item => <article className="rounded-2xl bg-white p-7 shadow-sm" key={item.name}><h2 className="text-xl font-semibold">{item.name}</h2><p className="mt-3 text-sm text-ink-muted">{item.copy}</p><p className="mt-6 text-3xl font-semibold text-brand">{item.price}</p></article>)}</div></section></main></PageShell>; }
