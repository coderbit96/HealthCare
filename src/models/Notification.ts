import { Schema, model, models } from "mongoose";
const NotificationSchema = new Schema({ recipientUid: { type: String, required: true, index: true }, event: { type: String, enum: ["appointment_booked", "appointment_reminder", "appointment_rescheduled", "appointment_cancelled", "lab_report_published", "prescription_created", "payment_received", "invoice_generated", "doctor_leave", "staff_leave_decision", "emergency_alert", "low_stock", "medicine_expiry"], required: true }, title: String, body: String, entityType: String, entityId: String, readAt: Date, channels: { inApp: { type: Boolean, default: true }, email: { type: Boolean, default: false }, sms: { type: Boolean, default: false }, push: { type: Boolean, default: false } } }, { timestamps: true });
export const Notification = models.Notification || model("Notification", NotificationSchema);
